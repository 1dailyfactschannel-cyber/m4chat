services:
  frontend:
    build:
      context: .
      dockerfile: artifacts/mockup-sandbox/Dockerfile
    container_name: m4chat-frontend
    environment:
      - NODE_ENV=production
      - PORT=8081
    ports:
      - "8081:8081"
    restart: unless-stopped